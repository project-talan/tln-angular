
module.exports = {
  tags: async (tln) => [],
  options: async (tln) => [],
  dotenvs: async (tln) => ['.env'],
  inherits: async (tln) => ['angular', 'nginx', 'docker'],
  depends: async (tln) => ['node-14.2.0', 'angular-8.3.28'],
  env: async (tln, env) => {},
  steps: async (tln) => [],
  components: async (tln) => []
}
